[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.32.0"
# CI backends to support
ci = "github"
# The installers to generate for each app
installers = ["shell", "homebrew"]
# A GitHub repo to push Homebrew formulas to
tap = "mentiora-ai/homebrew-loom"
# Brew formula name. Without this, cargo-dist names the formula
# after the package (`loom-cli.rb`) which forces `brew install
# mentiora-ai/loom/loom-cli`. We want the more natural
# `brew install mentiora-ai/loom/loom`.
formula = "loom"
# Publish jobs to run in CI
publish-jobs = ["homebrew"]
# Target platforms to build apps for (Rust target-triple syntax)
targets = ["aarch64-apple-darwin", "aarch64-unknown-linux-gnu", "x86_64-apple-darwin", "x86_64-unknown-linux-gnu"]
# Which actions to run on pull requests
pr-run-mode = "plan"
# Path that installers should place binaries in
install-path = "CARGO_HOME"
# Whether to install an updater program
install-updater = false
# Ship the generated action catalogue and man pages in the archive root.
# Postinstall (`loom postinstall`) reads embedded copies and writes them to
# `$PREFIX/share/man/man1/` so `man loom` works end-to-end. Files are
# regenerated from `loom-rpc/src/action_registry/` by the CI gate
# (.github/workflows/ci.yml's `gen-docs` job).
include = [
  "docs/actions.md",
  "docs/loom.1",
  "docs/loom-action.1",
]
