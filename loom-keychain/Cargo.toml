[package]
name = "loom-keychain"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
description.workspace = true
keywords.workspace = true
categories.workspace = true

# Feature-gated keychain access. `loom-core` stays platform-agnostic;
# this crate is the only place that pulls in OS-specific keychain deps.
# Future expansion (gated behind feature flags):
#   - macOS:   security-framework
#   - Linux:   secret-service
#   - Windows: windows-credentials

[dependencies]
zeroize = { workspace = true }

# Platform-specific deps under [target.'cfg(...)'.dependencies] go here
# once the per-platform backends are wired in.

[lints]
workspace = true
